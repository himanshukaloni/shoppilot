import {useEffect,useState} from 'react';import {ImagePlus} from 'lucide-react';import Modal from '../common/Modal';import {api} from '../../services/api';
const empty={name:'',sku:'',category:'General',stock:0,purchasePrice:0,sellingPrice:0,lowStockLimit:10};
export default function ProductModal({open,onClose,product,onSaved}){const[form,setForm]=useState(empty);const[file,setFile]=useState(null);const[busy,setBusy]=useState(false);const[error,setError]=useState('');useEffect(()=>{if(open){setForm(product?{name:product.name,sku:product.sku,category:product.category,stock:product.stock,purchasePrice:product.purchasePrice,sellingPrice:product.sellingPrice,lowStockLimit:product.lowStockLimit}:empty);setFile(null);setError('')}},[open,product]);const change=e=>setForm(f=>({...f,[e.target.name]:e.target.value}));const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const fd=new FormData();Object.entries(form).forEach(([k,v])=>fd.append(k,v));if(file)fd.append('image',file);const data=await api(product?`/products/${product._id}`:'/products',{method:product?'PUT':'POST',body:fd});onSaved(data);onClose()}catch(e){setError(e.message)}finally{setBusy(false)}};return <Modal open={open} onClose={onClose} title={product?'Edit product':'Add product'}><form onSubmit={submit}>{error&&<div className="error">{error}</div>}<div className="form-grid"><label>Product name<input className="input" name="name" value={form.name} onChange={change} required/></label><label>SKU / barcode<input className="input" name="sku" value={form.sku} onChange={change} required/></label><label>Category<select className="input" name="category" value={form.category} onChange={change}><option>General</option><option>Groceries</option><option>Beverages</option><option>Snacks</option><option>Personal Care</option><option>Household</option><option>Electronics</option></select></label><label>Opening stock<input className="input" type="number" min="0" name="stock" value={form.stock} onChange={change}/></label><label>Purchase price<input className="input" type="number" min="0" step="0.01" name="purchasePrice" value={form.purchasePrice} onChange={change}/></label><label>Selling price<input className="input" type="number" min="0" step="0.01" name="sellingPrice" value={form.sellingPrice} onChange={change}/></label><label>Low-stock limit<input className="input" type="number" min="0" name="lowStockLimit" value={form.lowStockLimit} onChange={change}/></label><label>Product image<input className="input" type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label></div>{product?.image?.url&&<img className="preview-image" src={product.image.url} alt="Current product"/>}<div className="upload-note"><ImagePlus size={15}/> Images are stored in ImageKit, not MongoDB.</div><div className="modal-actions"><button type="button" className="secondary" onClick={onClose}>Cancel</button><button className="primary" disabled={busy}>{busy?'Saving…':product?'Save changes':'Create product'}</button></div></form></Modal>}
