import {useState} from 'react';import {motion} from 'framer-motion';import {Store,ArrowRight} from 'lucide-react';import {api} from '../../services/api';
export default function Auth({onAuth}){const[mode,setMode]=useState('login');const[form,setForm]=useState({name:'',shopName:'',email:'',password:''});const[busy,setBusy]=useState(false);const[error,setError]=useState('');const change=e=>setForm(f=>({...f,[e.target.name]:e.target.value}));const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const data=await api(`/auth/${mode}`,{method:'POST',body:JSON.stringify(form)});onAuth(data.user)}catch(e){setError(e.message)}finally{setBusy(false)}};return <main className="auth-page"><div className="auth-glow one"/><div className="auth-glow two"/><motion.section className="auth-card" initial={{opacity:0,y:20}} animate={{opacity:1,y:0}}><div className="brand"><div className="logo"><Store size={19}/></div><div><b>ShopPilot</b><small>Retail OS</small></div></div><div className="auth-copy"><span>BUSINESS CONTROL CENTER</span><h1>{mode==='login'?'Run your shop smarter.':'Set up your shop.'}</h1><p>Inventory, sales, purchases, customers and profit — one clean workspace.</p></div>{error&&<div className="error">{error}</div>}<form onSubmit={submit}>{mode==='register'&&<><label>Full name<input className="input" name="name" value={form.name} onChange={change} required/></label><label>Shop name<input className="input" name="shopName" value={form.shopName} onChange={change} placeholder="My Store"/></label></>}<label>Email<input className="input" type="email" name="email" value={form.email} onChange={change} required/></label><label>Password<input className="input" type="password" name="password" value={form.password} onChange={change} minLength={6} required/></label><button className="primary full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create workspace'}<ArrowRight size={16}/></button></form><div className="auth-switch">{mode==='login'?'New to ShopPilot?':'Already have an account?'} <button onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}}>{mode==='login'?'Create account':'Sign in'}</button></div></motion.section></main>}
