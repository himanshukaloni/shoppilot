# ShopPilot Server

Express + MongoDB backend. Authentication uses an HTTP-only cookie. Product images are uploaded to ImageKit through the server so private ImageKit credentials never reach the browser.
